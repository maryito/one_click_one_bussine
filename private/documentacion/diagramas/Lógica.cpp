/*proveedor.(arreglo con el minimo tiempo)
proveedor.(arreglo con el minimo precio)

Finaliza recorrido

Eliminar proveedor(minimo tiempo)
Eliminar proveedor(minimo precio)

Inicia 2do recorrido
hacer
arreglo(proveedor con minimo precio)
    arreglo(proveedor con minimo tiempo)
    sino Eliminar (proveedor con minimo precio)
mientras arreglo(proveedor con minimo precio =! proveedor con minimo tiempo)
*/

int arreglo[2,4,5,6,1], arreglo2[10,151,4,5,6];
do
{
    int i,x,y;
    for ()
}
while()